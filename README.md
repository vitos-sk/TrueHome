# 🛒 TechHub - E-Commerce Platform

**TechHub** - это полнофункциональная платформа интернет-магазина электроники, построенная на современном стеке технологий. Приложение предоставляет полный цикл покупок: от просмотра каталога товаров до управления заказами через административную панель.

![TechHub](https://img.shields.io/badge/Status-Production%20Ready-success)
![Docker](https://img.shields.io/badge/Docker-Ready-blue)
![MongoDB](https://img.shields.io/badge/MongoDB-7.0-green)
![React](https://img.shields.io/badge/React-19.2-blue)
![Node.js](https://img.shields.io/badge/Node.js-18-alpine)

---

## 📋 Оглавление

- [Описание проекта](#-описание-проекта)
- [Основные возможности](#-основные-возможности)
- [Технологический стек](#-технологический-стек)
- [Архитектура](#-архитектура)
- [Быстрый старт](#-быстрый-старт)
- [Деплой на сервер](#-деплой-на-сервер)
- [API документация](#-api-документация)
- [Структура проекта](#-структура-проекта)

---

## 🎯 Описание проекта

TechHub - это современный интернет-магазин электроники, который позволяет пользователям:

- **Просматривать каталог товаров** с фильтрацией по категориям и поиском
- **Управлять корзиной покупок** с добавлением и удалением товаров
- **Регистрироваться и авторизовываться** с системой ролей (пользователь/администратор)
- **Оформлять заказы** с сохранением истории покупок

**Для администраторов:**

- Полное управление товарами (CRUD операции)
- Управление категориями
- Просмотр и управление пользователями
- Управление заказами

---

## ✨ Основные возможности

### Для пользователей:

- 🔍 Поиск товаров по названию
- 📂 Фильтрация по категориям
- 🛒 Корзина покупок
- 👤 Регистрация и авторизация
- 📦 История заказов
- 💳 Оформление заказов

### Для администраторов:

- ➕ Добавление/редактирование/удаление товаров
- 📁 Управление категориями
- 👥 Управление пользователями
- 📊 Просмотр статистики заказов
- 🔐 Административная панель

---

## 🛠 Технологический стек

### Frontend

- **React 19.2** - UI библиотека
- **Vite 7.1** - Сборщик и dev-сервер
- **Redux 5.0** - Управление состоянием
- **Redux Thunk** - Асинхронные действия
- **React Router DOM 7.9** - Маршрутизация
- **Styled Components 6.1** - CSS-in-JS стилизация
- **React Hook Form 7.63** - Управление формами
- **Yup 1.7** - Валидация форм
- **Nginx** - Production веб-сервер

### Backend

- **Node.js 18** - JavaScript runtime
- **Express 5.1** - Web framework
- **MongoDB 7.0** - NoSQL база данных
- **Mongoose 8.20** - ODM для MongoDB
- **JWT (jsonwebtoken)** - Аутентификация
- **Bcrypt 6.0** - Хеширование паролей
- **CORS** - Cross-Origin Resource Sharing
- **Cookie Parser** - Работа с cookies

### DevOps & Infrastructure

- **Docker** - Контейнеризация
- **Docker Compose** - Оркестрация контейнеров
- **MongoDB** - База данных в контейнере
- **Nginx** - Reverse proxy и статический сервер

---

## 🏗 Архитектура

Приложение построено по архитектуре **MVC (Model-View-Controller)** с разделением на три основных компонента:

```
┌─────────────────┐
│   Frontend      │  React + Redux (Port 80)
│   (Nginx)       │
└────────┬────────┘
         │ HTTP Requests
         ▼
┌─────────────────┐
│   Backend API   │  Express.js (Port 5001)
│   (Node.js)     │
└────────┬────────┘
         │ MongoDB Queries
         ▼
┌─────────────────┐
│   MongoDB       │  Database (Port 27017)
│   (Docker)      │
└─────────────────┘
```

### Компоненты:

1. **Frontend (React)**

   - Отображает UI
   - Управляет состоянием через Redux
   - Взаимодействует с API через HTTP запросы

2. **Backend (Express.js)**

   - RESTful API
   - Обработка бизнес-логики
   - Аутентификация и авторизация
   - Взаимодействие с базой данных

3. **Database (MongoDB)**
   - Хранение данных о товарах, пользователях, заказах
   - Коллекции: `products`, `categories`, `users`, `carts`, `orders`

---

## 🚀 Быстрый старт

### Предварительные требования

- **Docker Desktop** установлен и запущен
- **Docker Compose** (входит в Docker Desktop)
- **Git** для клонирования репозитория

### Локальный запуск

1. **Клонируйте репозиторий:**

   ```bash
   git clone <your-repo-url>
   cd TechHub
   ```

2. **Создайте файл `.env` в корне проекта:**

   ```bash
   cat > .env << EOF
   MONGO_URL=mongodb://mongodb:27017/techhub
   JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
   PORT=5000
   NODE_ENV=production
   EOF
   ```

3. **Запустите приложение:**

   ```bash
   docker-compose up -d --build
   ```

4. **Дождитесь запуска всех сервисов** (около 30 секунд)

5. **Откройте в браузере:**
   - **Frontend:** http://localhost
   - **Backend API:** http://localhost:5001/api/products

### Тестовые аккаунты

После первого запуска создайте тестовые данные:

```bash
# Заполнить базу данных тестовыми данными
docker-compose exec backend node seed.js
```

**Администратор:**

- Email: `admin@techhub.com`
- Password: `admin123`

**Пользователь:**

- Email: `user@techhub.com`
- Password: `user123`

---

## 🌐 Деплой на сервер

### Вариант 1: Деплой на VPS сервер (Ubuntu/Debian)

#### Шаг 1: Подготовка сервера

```bash
# Подключитесь к серверу по SSH
ssh user@your-server-ip

# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установите Docker Compose
sudo apt install docker-compose-plugin -y

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

#### Шаг 2: Клонирование проекта

```bash
# Установите Git (если не установлен)
sudo apt install git -y

# Клонируйте репозиторий
git clone <your-repo-url>
cd TechHub
```

#### Шаг 3: Настройка переменных окружения

```bash
# Создайте .env файл
nano .env
```

Добавьте следующие переменные (замените на свои значения):

```env
MONGO_URL=mongodb://mongodb:27017/techhub
JWT_SECRET=your-very-secure-random-secret-key-min-32-chars
PORT=5000
NODE_ENV=production
```

**Важно:** Используйте сильный случайный ключ для `JWT_SECRET`!

#### Шаг 4: Настройка docker-compose.yml для production

Обновите `docker-compose.yml` для использования вашего домена:

```yaml
frontend:
  environment:
    - VITE_API_URL=https://api.yourdomain.com # Замените на ваш домен
```

#### Шаг 5: Запуск приложения

```bash
# Запустите контейнеры
docker-compose up -d --build

# Проверьте статус
docker-compose ps

# Посмотрите логи
docker-compose logs -f
```

#### Шаг 6: Настройка Nginx (Reverse Proxy)

Установите Nginx на сервере:

```bash
sudo apt install nginx -y
```

Создайте конфигурацию:

```bash
sudo nano /etc/nginx/sites-available/techhub
```

Добавьте конфигурацию:

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # Frontend
    location / {
        proxy_pass http://localhost:80;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:5001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/techhub /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### Шаг 7: Настройка SSL (HTTPS) с Let's Encrypt

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получите SSL сертификат
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# Автоматическое обновление сертификата
sudo certbot renew --dry-run
```

#### Шаг 8: Настройка Firewall

```bash
# Разрешите необходимые порты
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

### Вариант 2: Деплой на облачные платформы

#### DigitalOcean / AWS / Google Cloud

1. Создайте VPS инстанс (Ubuntu 22.04)
2. Следуйте инструкциям из "Вариант 1"
3. Настройте домен и DNS записи

#### Heroku (альтернативный вариант)

```bash
# Установите Heroku CLI
# Создайте heroku.yml в корне проекта
```

---

## 📡 API Документация

### Базовый URL

```
http://localhost:5001/api
```

### Эндпоинты

#### Аутентификация

**Регистрация:**

```http
POST /api/register
Content-Type: application/json

{
  "login": "username",
  "email": "user@example.com",
  "password": "password123"
}
```

**Авторизация:**

```http
POST /api/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

**Выход:**

```http
POST /api/logout
```

#### Товары

**Получить все товары:**

```http
GET /api/products?page=1&limit=9&search=&category=all
```

**Получить товар по ID:**

```http
GET /api/products/:id
```

**Добавить товар (требуется авторизация админа):**

```http
POST /api/products
Authorization: Bearer <token>
Content-Type: application/json

{
  "name": "Product Name",
  "price": 9999,
  "description": "Description",
  "image": "https://example.com/image.jpg",
  "stock_quantity": 10,
  "category_id": "category_id"
}
```

**Обновить товар (требуется авторизация админа):**

```http
PATCH /api/products/:id
Authorization: Bearer <token>
```

**Удалить товар (требуется авторизация админа):**

```http
DELETE /api/products/:id
Authorization: Bearer <token>
```

#### Категории

**Получить все категории:**

```http
GET /api/categories
```

#### Корзина

**Получить корзину (требуется авторизация):**

```http
GET /api/cart
Authorization: Bearer <token>
```

**Добавить в корзину (требуется авторизация):**

```http
POST /api/cart
Authorization: Bearer <token>
Content-Type: application/json

{
  "productId": "product_id",
  "quantity": 1
}
```

**Удалить из корзины (требуется авторизация):**

```http
DELETE /api/cart/:productId
Authorization: Bearer <token>
```

---

## 📁 Структура проекта

```
TechHub/
├── backend/                 # Backend приложение
│   ├── controllers/         # Контроллеры (бизнес-логика)
│   │   ├── cart.js
│   │   ├── categories.js
│   │   ├── product.js
│   │   └── user.js
│   ├── models/              # Mongoose модели
│   │   ├── Cart.js
│   │   ├── Category.js
│   │   ├── Product.js
│   │   └── User.js
│   ├── routes/              # API маршруты
│   │   └── index.js
│   ├── middlewares/         # Middleware функции
│   │   ├── authenticated.js
│   │   └── hasRole.js
│   ├── helpers/            # Вспомогательные функции
│   │   ├── mapCart.js
│   │   ├── mapCategory.js
│   │   ├── mapProduct.js
│   │   ├── mapUser.js
│   │   └── token.js
│   ├── constants/           # Константы
│   │   └── role.js
│   ├── app.js              # Главный файл приложения
│   ├── Dockerfile          # Docker образ для backend
│   └── package.json
│
├── frontend/                # Frontend приложение
│   ├── src/
│   │   ├── pages/          # Страницы приложения
│   │   │   ├── main/       # Главная страница с каталогом
│   │   │   ├── product/   # Страница товара
│   │   │   ├── cart/      # Корзина
│   │   │   ├── admin-panel/ # Админ панель
│   │   │   ├── authorization/ # Авторизация
│   │   │   └── registration/  # Регистрация
│   │   ├── reducers/      # Redux редюсеры
│   │   ├── actions/       # Redux actions
│   │   ├── selectors/     # Redux селекторы
│   │   ├── ui-components/ # Переиспользуемые компоненты
│   │   ├── utils/         # Утилиты
│   │   └── store.jsx      # Redux store
│   ├── public/            # Статические файлы
│   ├── Dockerfile         # Docker образ для frontend
│   └── package.json
│
├── docker-compose.yml     # Конфигурация Docker Compose
├── .env                   # Переменные окружения (не в git)
└── README.md             # Этот файл
```

---

## 🔧 Полезные команды

### Docker команды

```bash
# Запуск приложения
docker-compose up -d

# Остановка приложения
docker-compose down

# Пересборка контейнеров
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f mongodb

# Проверка статуса
docker-compose ps

# Выполнение команд в контейнере
docker-compose exec backend sh
docker-compose exec mongodb mongosh techhub
```

### Работа с базой данных

```bash
# Подключение к MongoDB
docker-compose exec mongodb mongosh techhub

# Экспорт базы данных
docker-compose exec mongodb mongodump --db=techhub --out=/tmp/backup

# Импорт базы данных
docker-compose exec mongodb mongorestore --db=techhub /tmp/backup/techhub
```

---

## 🐛 Решение проблем

### Порт уже занят

Если порт 80, 5001 или 27017 занят, измените маппинг в `docker-compose.yml`:

```yaml
ports:
  - "8080:80" # Вместо 80:80
  - "5002:5000" # Вместо 5001:5000
```

### Backend не подключается к MongoDB

1. Проверьте, что MongoDB контейнер запущен:

   ```bash
   docker-compose ps
   ```

2. Проверьте логи:

   ```bash
   docker-compose logs mongodb
   docker-compose logs backend
   ```

3. Убедитесь, что все сервисы в одной сети (проверьте `docker-compose.yml`)

### Frontend не может подключиться к API

1. Проверьте переменную окружения `VITE_API_URL` в `docker-compose.yml`
2. Убедитесь, что backend доступен на указанном порту
3. Проверьте CORS настройки в `backend/app.js`

---

## 📝 Лицензия

Этот проект создан в образовательных целях.

---

## 👨‍💻 Автор

TechHub Development Team

---

## 🙏 Благодарности

- React Team
- Express.js Community
- MongoDB Team
- Docker Community

---

**Версия:** 1.0.0  
**Последнее обновление:** Декабрь 2025
